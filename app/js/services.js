'use strict';

/* Services */


// In this case it is a simple value service.
/*angular.module('myMusicLib.services', []).
  value('version', '0.1');*/

app.value('version', '0.2');

app.service('UserService', function () {
    /*this.getUserDetail = function (userID) {
	return user;
    };*/
});
